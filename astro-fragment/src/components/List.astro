---
interface Props {
    items: string[];
    name: string
}
const {items, name} = Astro.props;
---
<h2>Ich bin Liste {name}</h2>
<ul>
    {items.map((item: string) => (
            <li>{item}</li>
    ))}
</ul>

<style>
    ul {
        background-color: yellow;
    }
</style>